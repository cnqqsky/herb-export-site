# 依赖与本地产物
node_modules/
.wrangler/
data/
*.db
*.sqlite

# 旧版 Express 全栈代码（已迁移到 Cloudflare 原生，不再部署）
server/

# 敏感/本地环境变量（Cloudflare secrets 走 wrangler secret put，不入库）
.dev.vars
.env
.env.*

# npm 源配置：本地用国内镜像加速，但**不能入库**
# —— Cloudflare 构建机在海外，走 npmmirror 会显著变慢甚至超时；
#    仓库内保持官方源（package-lock.json 的 resolved 亦为 registry.npmjs.org）。
.npmrc

# 临时调试产物（Cloudflare API 探测等）
.cf*.json

# 本地遗留垃圾文件（旧 Express 版产物，可自行删除）
cleanup_done.txt
kill.txt
net.txt
server.log
test_region.js

# 系统
.DS_Store
Thumbs.db
public/uploads/
